<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/app.css') }}">
        {% endblock %}
    </head>
    <body>
    <div class="page-bar">
        <nav class="navbar ">
            <a class="navbar-brand" href="{{ path('app_homepage') }}">
                <p class="navbar-text">home</p>
            </a>
        </nav>
        <div class="spacer"></div>
        <nav class="navbar links">
            <a class="navbar-link" href="{{ path('app_homepage') }}">
                <p class="navbar-text link">traditional</p>
            </a>
            <a class="navbar-link" href="{{ path('app_homepage') }}">
                <p class="navbar-text link">digital</p>
            </a>
            <a class="navbar-link" href="{{ path('app_homepage') }}">
                <p class="navbar-text link">sketches</p>
            </a>
            <a class="navbar-link" href="{{ path('app_homepage') }}">
                <p class="navbar-text link">about me</p>
            </a>
        </nav>

    </div>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
